diff --git a/examples/x4mp/x4mp.cpp b/examples/x4mp/x4mp.cpp
index 3a9a76a..f69bb2f 100644
--- a/examples/x4mp/x4mp.cpp
+++ b/examples/x4mp/x4mp.cpp
@@ -605,7 +605,14 @@ static bool net_init_client(const char* ip, uint16_t port) {
         setsockopt(g_sock, IPPROTO_TCP, TCP_NODELAY, &one, sizeof(one));
         int r = connect(g_sock, (struct sockaddr*)&g_host_sa, sizeof(g_host_sa));
         if (r == 0) g_client_tcp_connecting = false;
-        else if (errno == EINPROGRESS) g_client_tcp_connecting = true;
+        // A non-blocking connect() that is still in flight reports EINPROGRESS
+        // on POSIX but WSAEWOULDBLOCK (10035, not WSAEINPROGRESS 10036) on
+        // Windows. Treating that as an error closed a perfectly good socket
+        // the instant the handshake started -- the host saw "client connected"
+        // followed by "recv=0 (CLIENT closed)" in the same millisecond, and no
+        // client could ever join from Windows. EWOULDBLOCK is EAGAIN on POSIX,
+        // which connect() never returns, so accepting both is safe everywhere.
+        else if (errno == EINPROGRESS || errno == EWOULDBLOCK) g_client_tcp_connecting = true;
         else { net_log("x4mp: net: connect() failed"); net_close_fd(g_sock, "init_client_tcp_connect"); g_sock = -1; return false; }
         g_net_client = true;
         g_client_last_recv = std::chrono::steady_clock::now();
@@ -751,7 +758,7 @@ static void net_client_tcp(NetClient& c) {
         snprintf(m, sizeof(m), "x4mp: net: TCP stream connected to %s:%u",
                  inet_ntoa(c.addr.sin_addr), (unsigned)c.stream_port);
         net_log(m);
-    } else if (errno == EINPROGRESS) {
+    } else if (errno == EINPROGRESS || errno == EWOULDBLOCK) {  // see net_start(): Windows says EWOULDBLOCK
         c.tcp_fd = fd; c.tcp_connecting = true; // verify next tick
     } else {
         net_close_fd(fd, "net_client_tcp_connect_fail"); c.tcp_backoff = 15;
@@ -1374,8 +1381,8 @@ static void net_send_client_objects(NetClient& c) {
     // Always send the COMPLETE region state every tick. LAN bandwidth is cheap,
     // and a full authoritative snapshot every tick keeps the client's view
     // exact — no delta-compression staleness to cause prune/respawn flicker.
-    memcpy(batch, "FULL 1\n", 6);
-    used = 6;
+    memcpy(batch, "FULL 1\n", 7);
+    used = 7;
     // Append one OBJ line (handles the batch flush).
     auto append = [&](ZoneObj* zo) {
         UniverseID obj = (UniverseID)zo->id;
@@ -2885,6 +2892,12 @@ static void on_host_request(const char* /*event_name*/, void* data, void* /*user
     g_api->log(X4NATIVE_LOG_INFO, "x4mp: HOST requested (in-game menu)");
     g_auto = "host";                       // set role at runtime (menu-driven)
     if (module && *module) g_module = module;
+    // Starting a game reloads the game's Lua state, which makes x4native
+    // re-discover and RESTART this extension -- every global here, including
+    // g_auto, is destroyed and the listening socket is closed. Park the request
+    // in the process environment so the next x4native_init() can resume it.
+    x4mp_setenv("X4MP_RESUME_ROLE", "host");
+    if (!g_module.empty()) x4mp_setenv("X4MP_RESUME_MODULE", g_module.c_str());
     net_start();                           // start our custom network (UDP 7777) now
     if (!g_save.empty()) {
         g_save_pending = true;             // load the save first; do_host on game loaded
@@ -2909,6 +2922,10 @@ static void on_join_request(const char* /*event_name*/, void* data, void* /*user
     g_api->log(X4NATIVE_LOG_INFO, "x4mp: JOIN requested (in-game menu)");
     g_auto = "client";                     // set role at runtime (menu-driven)
     if (ip && *ip) { g_server_ip = ip; g_net_host_ip = ip; }
+    // See on_host_request(): survive the extension restart that the save load
+    // triggers.
+    x4mp_setenv("X4MP_RESUME_ROLE", "client");
+    if (!g_net_host_ip.empty()) x4mp_setenv("X4MP_RESUME_IP", g_net_host_ip.c_str());
     net_start();                           // connect our custom network (UDP 7777) now
     if (!g_save.empty()) {
         g_save_pending = true;             // load the same save; client logic connects
@@ -3289,6 +3306,33 @@ X4NATIVE_EXPORT int x4native_init(X4NativeAPI* api) {
     // them to the host over the control channel.
     g_sub_send_act = api->subscribe("x4mp.send_act", on_send_act, nullptr, api);
 
+    // Resume a menu-driven host/join across the extension restart that the
+    // game's Lua-state reload causes (see on_host_request). X4MP_AUTO, when
+    // set, wins -- it means the launcher asked for a role explicitly.
+    //
+    // Unlike the X4MP_AUTO paths below we deliberately do NOT schedule another
+    // save load / new game here: the click that preceded this reload already
+    // started one. We only restore the role (net_start() at the end of this
+    // function re-opens the listener / reconnects) and mark the load as
+    // in-flight so on_game_loaded finishes the job -- do_host() for a host,
+    // wait-for-universe-ready for a client.
+    if (g_auto.empty()) {
+        const char* resume = std::getenv("X4MP_RESUME_ROLE");
+        if (resume && *resume) {
+            g_auto = resume;
+            const char* rip = std::getenv("X4MP_RESUME_IP");
+            if (rip && *rip) { g_server_ip = rip; g_net_host_ip = rip; }
+            const char* rmod = std::getenv("X4MP_RESUME_MODULE");
+            if (rmod && *rmod) g_module = rmod;
+            if (!g_save.empty()) g_save_loading = true;
+            char rbuf[256];
+            snprintf(rbuf, sizeof(rbuf),
+                     "x4mp: resuming %s after extension reload (in-game menu request)",
+                     g_auto.c_str());
+            api->log(X4NATIVE_LOG_INFO, rbuf);
+        }
+    }
+
     // Optional env-driven fallback (OFF by default).
     if (g_auto == "client") {
         // THIN CLIENT (Option 1 intermediate): load the SAME save as the host
diff --git a/examples/x4mp_platform.h b/examples/x4mp_platform.h
index 81ca750..92302ec 100644
--- a/examples/x4mp_platform.h
+++ b/examples/x4mp_platform.h
@@ -77,6 +77,9 @@
 #define EINTR                   WSAEINTR
 #define EAGAIN                  WSAEWOULDBLOCK
 #define EWOULDBLOCK             WSAEWOULDBLOCK
+// NOTE: a non-blocking connect() in flight reports WSAEWOULDBLOCK on Windows,
+// NOT WSAEINPROGRESS. Code checking for a pending connect must accept
+// EWOULDBLOCK too -- see the connect sites in x4mp.cpp.
 #define EINPROGRESS             WSAEINPROGRESS
 #define EBADF                   WSAEBADF
 #define EPIPE                   WSAECONNRESET
@@ -89,7 +92,10 @@ static inline int x4mp_sock_set_nonblocking(int fd) {
 }
 
 static inline const char* x4mp_winsock_strerror(int e) {
-    char buf[256];
+    // NOTE: must NOT be a plain local -- the caller reads the pointer after we
+    // return. thread_local keeps it valid and keeps the net threads from
+    // clobbering each other's message.
+    static thread_local char buf[256];
     if (FormatMessageA(FORMAT_MESSAGE_FROM_SYSTEM | FORMAT_MESSAGE_IGNORE_INSERTS,
                        0, (DWORD)e, 0, buf, (DWORD)sizeof(buf), 0) == 0) {
         buf[0] = 0;
@@ -109,6 +115,15 @@ static inline int x4mp_sock_ensure_init(void) {
     return 0;
 }
 
+// Set a variable in THIS PROCESS's environment. x4native tears the extension
+// down and re-initializes it whenever the game reloads its Lua state (a save
+// load), which destroys all of our globals; the environment survives because
+// it belongs to the process, not to this DLL. See the resume logic in
+// x4native_init().
+static inline int x4mp_setenv(const char* name, const char* value) {
+    return _putenv_s(name, value ? value : "");
+}
+
 // dirent stubs: net_dump_fds() is Linux-only diagnostics (reads /proc/self/fd).
 // opendir() returns null so the function no-ops on Windows.
 typedef void DIR;
@@ -137,4 +152,10 @@ static inline ssize_t         readlink(const char*, char*, size_t) { return -1;
 // POSIX: WinSock is not a thing; socket() works immediately.
 static inline int x4mp_sock_ensure_init(void) { return 0; }
 
+// See the Windows branch: survives the extension reload because it lives in
+// the process, not in this DLL.
+static inline int x4mp_setenv(const char* name, const char* value) {
+    return setenv(name, value ? value : "", 1);
+}
+
 #endif
diff --git a/examples/x4mp_stream/x4mp_stream.cpp b/examples/x4mp_stream/x4mp_stream.cpp
index b03e7f8..31f04d2 100644
--- a/examples/x4mp_stream/x4mp_stream.cpp
+++ b/examples/x4mp_stream/x4mp_stream.cpp
@@ -78,6 +78,10 @@ struct RemoteObj {
     bool has_target = false;
     bool spawned = false;
     bool is_player = false;                            // true for PLAYER ghosts (human ships)
+    // The ghost model is chosen once, at spawn. When the host swaps ship the
+    // macro on the wire changes, and without this the client keeps flying the
+    // OLD model around at the new position for the rest of the session.
+    bool macro_changed = false;                        // respawn the ghost with the new macro
     bool is_station = false;                           // true for STA ghosts (host-built stations)
     // Glide (sector-entry convergence without a teleport): when a local ship
     // is bound while diverged from the host position by <= g_glide_max_m,
@@ -546,6 +550,9 @@ static void parse_line(char* line) {
             RemoteObj& o = g_objs[cid];
             if (o.has_target && !o.gliding) { o.px = o.tx; o.py = o.ty; o.pz = o.tz; }
             o.host_id = cid;
+            // Host swapped ship? The spawned ghost is the wrong model now.
+            if (o.spawned && macro[0] && o.macro[0] && strcmp(o.macro, macro) != 0)
+                o.macro_changed = true;
             snprintf(o.macro, sizeof(o.macro), "%s", macro[0] ? macro : "?");
             snprintf(o.faction, sizeof(o.faction), "%s", faction[0] ? faction : "player");
             if (nf >= 10 && sectormacro[0] && sectormacro[0] != '?')
@@ -1306,6 +1313,31 @@ static void render_pass() {
         }
 
         // ---- Ghost path (PLAYER ghosts + host-only ships) -----------------
+        // The macro changed under an existing ghost (the host swapped ship):
+        // drop it here and let the spawn below rebuild it with the right model.
+        // PlayerShipIDs guards the client's OWN ship, which must never be
+        // removed (that triggers Game Over: killmethod=removed).
+        if (o.spawned && o.macro_changed) {
+            o.macro_changed = false;
+            if (o.client_id && g_game->RemoveComponent && g_game->IsValidComponent &&
+                g_game->IsValidComponent(o.client_id)) {
+                PlayerShipIDs pids;
+                if (!pids.contains(o.client_id)) {
+                    g_game->RemoveComponent(o.client_id);
+                    char dbg[220];
+                    snprintf(dbg, sizeof(dbg),
+                             "x4mp_stream: [DBG] ghost %llu macro changed -> respawning as %s",
+                             (unsigned long long)kv.first, o.macro);
+                    net_log(dbg);
+                    o.client_id = 0;
+                    o.spawned = false;
+                }
+            } else {
+                o.client_id = 0;
+                o.spawned = false;
+            }
+        }
+
         if (!o.spawned) {
             UniverseID newid = g_game->SpawnObjectAtPos2(o.macro, target_sector, pos,
                                                          (o.faction[0]) ? o.faction : "player");
