# ===========================================================================
# x4llm settings — board computer LLM parameters
#
# Edit this file and the changes apply LIVE on the next "AI Board Computer"
# query (no game restart needed). Lines are key=value. Blank lines and lines
# starting with '#' are ignored. Any key you omit keeps its current value
# (environment variable or built-in default).
#
# Sampling values follow llama.cpp semantics:
#   * top_k:            number of highest-probability tokens to consider (-1 = off)
#   * top_p:            nucleus sampling threshold (1.0 = off)
#   * min_p:            minimum probability relative to top token (0.0 = off)
#   * top_n_sigma:      N-sigma sampling (0.0 = off)
#   * presence_penalty: nudges toward new topics (0.0 = off)
#   * typical_p:        local typicality threshold (1.0 = off)
#   * repeat_penalty:   discourages repetition (1.0 = off; ~1.1 typical)
# ===========================================================================

# --- endpoint -------------------------------------------------------------
llm_url=http://192.168.1.101:5001/v1
model=local

# --- sampling (main) ------------------------------------------------------
temperature=0.7
top_k=20
top_p=0.95
min_p=0.02
top_n_sigma=0.0

# --- sampling (advanced / optional) ---------------------------------------
presence_penalty=0.0
typical_p=1.0
repeat_penalty=1.0

# --- generation -----------------------------------------------------------
max_tokens=1024
# enable reasoning ("thinking") tokens: 1 = on, 0 = off (off is faster)
enable_thinking=0

# --- persona (system prompt) ----------------------------------------------
# One line. The context-awareness (target name etc.) is appended by the UI.
system=You are the ship's onboard computer (board computer) for X4: Foundations. Be concise, helpful and in-character. Answer directly with short sentences.
